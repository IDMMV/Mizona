import { useEffect, useState } from 'react';import Card from '../../components/Card';import { supabase } from '../../lib/supabase';
export default function Communities(){const[rows,setRows]=useState([]);useEffect(()=>{supabase.from('mz_communities').select('*,institution:mz_institutions(name)').eq('status','active').order('created_at',{ascending:false}).then(({data})=>setRows(data||[]))},[]);return <div className="page"><div className="pageTitle"><div><h1>Comunidades</h1><p className="muted">Únete a grupos de estudio, clubes y proyectos.</p></div></div><div className="grid3">{rows.map(row=><Card key={row.id} title={row.name} icon="👥"><p>{row.description||'Comunidad estudiantil'}</p><small>{row.community_type} · {row.institution?.name||'Abierta'}</small></Card>)}</div>{!rows.length&&<Card title="Aún no hay comunidades" icon="🌱"><p>Cuando se creen comunidades verificadas aparecerán aquí.</p></Card>}</div>}
