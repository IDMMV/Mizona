import { useEffect, useState } from 'react';import Card from '../../components/Card';import { supabase } from '../../lib/supabase';
export default function Institutions(){const[rows,setRows]=useState([]);useEffect(()=>{supabase.from('mz_institutions').select('*').eq('verification_status','verified').order('name').then(({data})=>setRows(data||[]))},[]);return <div className="page"><div className="pageTitle"><div><h1>Instituciones</h1><p className="muted">Conoce experiencias reales de colegios, institutos y universidades.</p></div></div><div className="grid3">{rows.map(row=><Card key={row.id} title={row.name} icon="🏫"><p>{row.description||'Institución verificada en MiZona.'}</p><small>{row.institution_type} · {row.city||row.country}</small></Card>)}</div>{!rows.length&&<Card title="Sin instituciones verificadas" icon="🏫"><p>El administrador podrá registrar y verificar instituciones desde Supabase.</p></Card>}</div>}
